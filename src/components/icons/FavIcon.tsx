import { View, Text } from "react-native";
import React from "react";
import IconFactory from "./shared/IconFactory";

export default function FavIcon() {
  return <IconFactory type="heart-outline" />;
}
